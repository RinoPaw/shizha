[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "anti-fraud-explorer"
version = "0.2.0"
description = "识诈 — 面向公众的反诈案例检索与对话应用"
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
    "edge-tts>=7.2,<8",
    "fastapi>=0.141,<1",
    "httpx>=0.28,<1",
    "pypinyin>=0.51,<1",
    "uvicorn[standard]>=0.35,<1",
    "websockets>=15,<17",
]

[dependency-groups]
dev = [
    "httpx2>=2.12.0",
    "pytest>=8,<9",
    "ruff>=0.12,<1",
]

[project.scripts]
anti-fraud-explorer = "anti_fraud_explorer.api:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
